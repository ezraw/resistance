# Project-Specific Instructions

## Git Configuration

This is a personal project. Always use the personal GitHub account for commits:

- **Name**: Ezra Wolfe
- **Username**: ezraw
- **Email**: ezraw@yahoo.com

Before committing, verify the git config is set correctly:
```bash
git config user.name   # Should be "Ezra Wolfe"
git config user.email  # Should be "ezraw@yahoo.com"
```

If incorrect, set it with:
```bash
git config user.name "Ezra Wolfe"
git config user.email "ezraw@yahoo.com"
```

## UI Style Guide

When working on any UI component, ALWAYS read and follow the style guide at `docs/design/STYLE_GUIDE.md`.
